{
    //template lits

    /*

    let arrowFunc = () => {
        console.log('hello world')
    }

    arrowFunc()

    */

    /*
    let nameGreeting = (name) => {
        console.log("Hello " + name)
    }

    nameGreeting('Sonia')
    

    let add = (a, b) => {
        return a +b 
    }
    console.log(add(3,4))

       
    names.map((name)=>{console.log(`howdy ${name}`)})


    let names = ['david', 'fred', 'clinton', 'alex']
    let i = 0

    names.map((name)=>{
        i++;
        console.log(`howdy ${name} ${i}`)
    })

    

    let name = 'Bob'

    console.log(`Hi ${name}`)

    let paragraph = `
        The trail to the left had a "Danger! Do Not Pass" 
    sign telling people to take the trail to the right. 
    This wasn't the way Zeke approached his hiking. 
        Rather than a warning, 
    Zeke read the sign as an invitation to explore an area 
    that would be adventurous and exciting. 
        As the others in the group all shited to the right, 
    Zeke slipped past the danger sign 
    to begin an adventure he would later regret.`

    console.log(paragraph)

    
   function getReasonsCount () {return 1;}

   let interpolation = `Give me ${ (getReasonsCount() == 2) ? 'one reason': 'few  reasons'} to try this`
   console.log(interpolation)

   

    let names = ['david', 'fred', 'clinton', 'alex']
    let i = 0

    var transformed = names.map((name)=>{
        i++
        return `${name} > ${i}`
    })

    console.log(transformed)
    */
    let names = ['david', 'fred', 'clinton', 'alex']

    var transformed = names.map()
    console.log(transformed)





}